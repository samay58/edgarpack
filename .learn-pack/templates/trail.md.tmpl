# Trail {{N}}: {{SHORT_IMPERATIVE_TITLE}}

Time: about {{MINUTES}} minutes.

A one-paragraph hook. What concrete action does this trail follow? Why is this the right starting point for what it covers? If it builds on another trail, say so in this sentence and link it, then move on. Second person, present tense.

## Try it

Lead with the command the reader runs, then explain what came back.

```bash
{{COMMAND}}
```

Describe the output the reader is now looking at: the value, the file, the table. Point them at what to notice in it. This is the spine of the trail, not decoration.

## {{PLAIN_DESCRIPTIVE_STEP_HEADER}}

What happens, in plain language. The reader did X; the system reacts. Describe what the code does and cite the line. Do not paste the function body. The citation is the way in.

A short command or a few lines of representative output earn their space here. Implementation source does not. If you feel the urge to paste a function, write one sentence about what it does instead and trust the line reference.

## {{NEXT_PLAIN_DESCRIPTIVE_STEP_HEADER}}

Same shape. One concrete thing happening, and what it leads to. If this step needs context another trail already covered, link to that trail rather than restating it.

## In the code

Every claim above that came from the source points here. One list, at the end, with real line numbers. No `**Code**:` labels scattered through the prose.

- `path/to/file.ext:LINE` defines {{WHAT_LIVES_THERE}}.
- `path/to/file.ext:LINE` runs {{WHAT_HAPPENS_THERE}}.
