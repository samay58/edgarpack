# Salvage Matrix

Note: this matrix began as a conservative assessment. After the user explicitly chose a full clean rewrite, KEEP means "preserve the validated behavior/learning," not "reuse the current implementation shape."

| Item | Bucket | Evidence | Rationale | Risk if removed | Validation |
| --- | --- | --- | --- | --- | --- |
| CLI-first product surface (`build`, `query`, `comps`, `compare`, `which`, `diff`, `timeline`) | KEEP | `README.md:36-85`, `README.md:153-186`; CLI help passed; pytest passed | This proves the product should stay CLI-first and evidence-first. vNext should keep the jobs-to-be-done, not the old command taxonomy. | Discarding the learning would lose mature behavior and broad tests. | vNext CLI contract tests plus selected old CLI parity smokes. |
| Deterministic SEC pack builder and manifest shape | KEEP | `edgarpack/pack/build.py:111-305`; `edgarpack/pack/manifest.py:78-91`, `edgarpack/pack/manifest.py:131-154` | Core artifact contract is coherent: clean markdown, sections, llms.txt, hashes. | Downstream diff/search/query fixtures would break. | Pack determinism test lane (`docs/TESTING.md:76-84`) and `tests/test_pack_build.py`. |
| Citation data model (`CitedValue`, `DerivedValue`, query outputs) | KEEP | `README.md:21`, `README.md:49`; `edgarpack/query/models.py`; LTM test harness `tests/conftest.py:55-98` | Source traceability is the product's trust boundary. | Claim outputs become unverifiable. | Query JSON/full JSON smoke; LTM component citation assertions. |
| Query period and derived metric semantics | KEEP | `docs/learn/manifest.yml:78-96`, `docs/TESTING.md:122-130`; broad pytest coverage | Complex but validated, especially LTM and component citations. | Financial outputs regress silently. | `pytest tests/test_periods.py tests/test_financials.py tests/test_stress.py -q`. |
| Diff/timeline observatory CLI | KEEP | `README.md:203-215`; `docs/OBSERVATORY.md`; tests passed | Useful derived layer over packs; not the architecture base. | Lose validated disclosure-change workflow. | `pytest tests/test_diff.py tests/test_registration_timeline.py tests/test_cli_registration_timeline_render.py -q`. |
| HKEX query path for committed prospectus fixtures | KEEP | `docs/china-lens/IMPLEMENTATION_TRACKER.md:43-63`; `tests/fixtures/china_packs/README.md:7-13`; golden harness docs | Validated for MiniMax/Zhipu-style packs and cross-market compare. | Cross-market compare loses China support already tested. | `pytest tests/test_china_query_hk.py tests/test_china_query_eval.py -q`. |
| SSE build/translate pipeline on main | KEEP | `README.md:178-181`; `docs/china-lens/IMPLEMENTATION_TRACKER.md:27-41`; `edgarpack/pack/build.py:329-460` | Contrary to stale backlog, this is shipped and tested. | Reverting to stale branch mental model would lose current functionality. | `pytest tests/test_sse_pack.py tests/test_sectionize_cn.py tests/test_translation_validators.py -q`; `edgarpack build-sse --help`. |
| China deterministic QA validators | KEEP | `edgarpack/china/qa/validators.py:16-137`; `tests/test_china_validators.py` | Enforces the mission: missing/bad citations become unsupported. | API/web could publish unsupported findings. | `pytest tests/test_china_validators.py -q`. |
| Learn pack docs (`docs/learn/`) | KEEP | `docs/learn/manifest.yml:15-34`, `docs/learn/manifest.yml:38-163` | Captures validated code navigation and helps future agents avoid blind rewrites. | Future changes take longer and repeat old mistakes. | Spot-check line references after large refactors; run `/learn-pack` only when intentionally refreshing. |
| Literal core CLI command names (`build`, `query`, `diff`, `timeline`, `search`, `index`, `cache`, `doctor`) | PORT IDEA | `edgarpack --help`; parser definitions in `edgarpack/cli.py:214-309`, `edgarpack/cli.py:377-576` | These names are useful evidence, but vNext should use a smaller evidence-verb grammar: `filings`, `pack`, `cite`, `audit`, then later `compare` and `trace`. | Blindly preserving old names carries forward avoidable surface-area drift. | vNext help/JSON contract tests and manual workflow smoke. |
| Flat top-level CLI help and mixed command taxonomy | REWRITE | `edgarpack --help`; parser definitions in `edgarpack/cli.py:212-774` | The approved clean rewrite should start from the workflow grammar instead of aliasing the old shape first. | Leaving it flat makes agents/humans guess between adjacent commands. | CLI help contract tests for `edgarpack-next`. |
| `which` qualitative-KPI command | PORT IDEA | `edgarpack/cli.py:671-705`; open bead `edgarpack-t2h` touches its empty-state UX | Behavior is useful, but the name is not self-explanatory outside docs. | Removing it loses validated KPI discovery; keeping only this name hurts discoverability. | Add non-breaking alias such as `kpis` or `discover-kpis`; test same output. |
| `comps` and `compare` command split | PORT IDEA | `edgarpack/cli.py:578-630`, `edgarpack/cli.py:707-733` | Both are useful, but their distinction is not obvious from names alone. | Users may pick the wrong comparison surface and misread output/currency behavior. | Clarify help and consider `peers` alias for `comps`; snapshot representative outputs. |
| `learned` registry command name | PORT IDEA | `edgarpack/cli.py:631-669` | The registry is real, but the command exposes an implementation term. | Humans may not find metric mapping maintenance; agents may misuse it as product data. | Add `mappings` alias or rename only with deprecation period. |
| FastAPI China Lens workspace | PARK | `docs/china-lens/IMPLEMENTATION_TRACKER.md:5-7`, `docs/china-lens/IMPLEMENTATION_TRACKER.md:65-80`, `docs/china-lens/IMPLEMENTATION_TRACKER.md:112-123` | Contracts exist and tests pass, but product status is parked. | Deleting now loses optional future workspace; keeping active in roadmap creates distraction. | If revived, run China Lens local loop (`docs/TESTING.md:132-171`) plus API tests. |
| Next.js Evidence Explorer / Observatory web | PARK | `web/` present; tracker says frontend integration still needs plumbing (`docs/china-lens/IMPLEMENTATION_TRACKER.md:119`) | Treat as prototype/client, not core. | Deleting loses UI ideas; active development without product decision creates churn. | If revived, replace sample data with fixtures, run `npm run build`, accessibility checks, and API-backed smoke. |
| Demo sample data in `web/lib/sample-data.ts` | DELETE | Hardcoded claims/citations at `web/lib/sample-data.ts:36-146`; AGENTS forbids invented facts | Demo-only claims are unsafe in a citation-first product. | Removing without replacement makes demo mode empty. | Replace with committed fixture-derived JSON; assert demo supported findings cite fixture chunk IDs. |
| Default seeded China Lens mock data | PORT IDEA | `edgarpack/china/service.py:86-189` | Useful for local loop, but should be fixture-backed and clearly non-production. | Removing immediately can break local API demos/tests. | Move seed payload into test/demo fixtures; production default should not seed unless explicitly enabled. |
| CNINFO manifest ingestion | PORT IDEA | Manifest models at `edgarpack/china/acquire/cninfo.py:17-44`; sync path at `edgarpack/china/service.py:620-742`; tracker says live fetch is stubbed (`docs/china-lens/IMPLEMENTATION_TRACKER.md:116`) | Deterministic manifest ingestion is valuable; live fetch is not real yet. | Deleting loses the safest ingestion path. | Fixture manifest E2E: sync -> search -> pack -> citation resolution. |
| OCR fallback placeholder | PARK | `edgarpack/china/extract/pdf_extract.py:33-47`; tracker open item says production OCR provider is parked (`docs/china-lens/IMPLEMENTATION_TRACKER.md:117`) | Placeholder is honest scaffolding, not product capability. | Removing may simplify, but tests/local flow might rely on low-confidence marker. | Keep explicit "no OCR executed" status; revive only with provider tests and source page images. |
| PostgreSQL/vector retrieval ambitions | PARK | Storage supports postgres JSONB (`edgarpack/china/storage.py`); tracker says vector/database-native search is parked (`docs/china-lens/IMPLEMENTATION_TRACKER.md:118`) | Not required for current CLI. | Premature deletion may hurt future API; active work would distract. | Revive with integration tests against disposable DB and retrieval quality fixtures. |
| Strict mypy baseline | PARK | `pyproject.toml:64-66`; mypy fails with 1293 errors; tracker parks restoration (`docs/china-lens/IMPLEMENTATION_TRACKER.md:121-123`) | Valuable eventually, not a current release gate. | Deleting config hides intent. | Define scoped mypy target first; ratchet errors down. |
| Stale `docs/BACKLOG.md` SSE parked note | DELETE | `docs/BACKLOG.md:7-15` conflicts with README/tracker/code | It misleads future agents. | Deleting entire backlog could lose LTM UX note. | In build mode, update/remove stale SSE section and preserve valid LTM UX item if still wanted. |
| Old-command compatibility as a first-slice requirement | DELETE | User chose a fresh `edgarpack-next` command and clean grammar | Compatibility aliases would optimize for the old surface before the new one proves itself. | Carrying old names forward can preserve the confusion the rewrite is meant to remove. | Add aliases only after vNext stabilizes and the user approves them. |
| Large-module split for its own sake | DELETE | Bead trim closed pure refactors (`docs/superpowers/specs/2026-04-20-bead-backlog-trim-design.md:20-32`) | Size alone is not a reason to rewrite a green core. | None if behavior-driven extractions still allowed. | Require each extraction to come with failing/coverage tests and a user story. |
| HKEX annual-report ingestion | REWRITE | `bd show edgarpack-sfi`; current adapter is prospectus-shaped (`edgarpack/hk/adapter.py:57-145`) | Need new document-shape support, not a tweak to IPO headings. | Without it, mature HKEX companies are overclaimed. | Add annual-report fixtures for Tencent/Meituan before implementation. |
| Empty discovered-KPI series rendering | PORT IDEA | `bd show edgarpack-t2h` | Useful narrow UX fix; implementation likely small. | Existing awkward output remains. | Add CLI test for empty series showing `(no matching periods)` or N/A. |
| Section-only extraction claims | PORT IDEA | Benchmark limitation at `docs/BENCHMARKS.md:90-127` | Full-filing compression works; section targeting needs stronger guards. | Removing section files breaks pack UX. | Add incorporation-by-reference fixtures and minimum content assertions for core sections. |
| Clean rewrite of the repo | REWRITE | User explicitly selected full clean rewrite after the assessment; see `.reforge/08_rebuild_plan.md` and the Superpowers spec | The rewrite is now the product decision. The guardrail is to preserve validated learning through fixtures, parity checks, and reviewed leaf utility reuse. | A rewrite that ignores old evidence would lose financial edge cases and citation semantics. | Execute the vNext plan TDD-first, then require offline gates, live SEC smoke, and selected parity checks before cutover. |
| Unknown current product priority for China Lens | UNKNOWN | AGENTS mission emphasizes China Lens, while tracker parks API/web and says CLI primary | Requires user/product decision. | Choosing wrong priority wastes the next cycle. | Ask before build mode: "Revive China Lens UI/API, or harden CLI-first EdgarPack?" |
