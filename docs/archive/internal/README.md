# Internal Archive

This folder holds historical planning docs that are useful context but not current user guidance.

The active product path is the existing `edgarpack` CLI and docs. Archived rewrite or experiment plans here should not be treated as roadmap unless they are explicitly revived in a new bead.
