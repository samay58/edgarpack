# EdgarPack Friction Log

Record product friction from using EdgarPack heavily. File follow-up beads for material issues.

| category | command_or_file | observed_behavior | expected_behavior | impact | bead_id | status |
| --- | --- | --- | --- | --- | --- | --- |
| local-runtime | `uv run edgarpack identify <ticker>` under default sandbox | Command failed before EdgarPack started: `failed to open file /Users/samaydhawan/.cache/uv/sdists-v9/.git: Operation not permitted`. | Agent workflows should have a documented hermetic invocation path or a project-local cache option that avoids false tool failures. | Raw evidence files can silently contain environment errors unless checked before notes are written. | TBD | candidate |
| filing-discovery | `edgarpack list <ticker> --form 10-K` for SHOP/TEAM | 10-K-only listing made SHOP and TEAM look like they lacked usable annual history until 40-F/20-F/F-1 were queried separately. | Investor workflow should offer an annual-report discovery mode that follows issuer form-family changes automatically. | Researchers must already know foreign-issuer form families, which weakens EdgarPack as an investor copilot. | edgarpack-9mb.1 | filed |
| metric-discovery | `edgarpack query CRM subscription_revenue ...` | CLI rejected `subscription_revenue` and suggested `subscription_rev`; metric naming requires prior local knowledge. | Investor-facing query mode should expose discoverable metric aliases and SaaS presets, especially for ARR/subscription/RPO/SBC metrics. | Slows analyst workflow and increases risk of failed raw evidence captures. | edgarpack-9mb.3 | filed |
| historical-query-coverage | `edgarpack query TEAM ... --period lfy,lfy-5,lfy-10` | Current 10-K metrics are strong, but historical Atlassian `lfy-5`/`lfy-10` query coverage is sparse despite 20-F/F-1 filings existing through `list`. | Query should bridge form-family history or clearly explain why historical XBRL/pack extraction is unavailable. | Investor cannot rely on standard query alone for public-company history across issuer transitions. | edgarpack-9mb.1 / edgarpack-9mb.4 | filed |
| period-selector-safety | `edgarpack query ... --period lfy,lfy-5,lfy-10` | Sparse `lfy-10` outputs can contain recent fiscal years or partial metrics when true 10-year history is unavailable. | Multi-period query output should label unavailable periods explicitly and warn when returned facts do not match the intended lookback. | A naive analyst can mislabel a partial recent fact as a 10-year baseline. | edgarpack-9mb.4 | filed |
| kpi-discovery-runtime | `for ticker in ...; do edgarpack which "$ticker" --format json; done` | Default full-cohort batch ran for several minutes and stalled around SNOW/HUBS, requiring process termination and targeted fallbacks. | `which` should expose progress, timeouts, per-filing skip reasons, and a resumable batch mode. | Unattended investor research workflows become brittle and hard to trust. | edgarpack-9mb.5 | filed |
| kpi-evidence-locators | `edgarpack which CRM --format json` after `build CRM --with-chunks --force` | CRM discovered KPI rows include section IDs but still have null `chunk_id`. | If packs are built with chunks, every discovered KPI row should carry a stable chunk ID or explicit reason why not. | Citation-backed report writing cannot uniformly deep-link every KPI claim. | edgarpack-9mb.5 | filed |
| json-output-contract | `edgarpack which <ticker> --format json > which-<ticker>.json` | CLI writes resolver/progress text before the JSON object, so redirected files are not parseable JSON without post-processing. | JSON mode should emit machine-readable JSON on stdout and progress/logging on stderr. | Source-ledger artifacts can be mislabeled as JSON even though `json.loads` fails. | edgarpack-9mb.2 | filed |
